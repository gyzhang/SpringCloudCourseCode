server:
  port: 8088
spring:
  application:
    name: eureka-hystrix-dashboard
eureka:
  client:
    service-url:
      default-zone: http://p1:8761/eureka/
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
